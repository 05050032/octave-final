function [out]=caps(str)
  str=lower(str);
  sstr(1)=upper(srt(1));
  n=length(str);
  for ii=2:n
    if isspace(str(ii-1))&isletter(str(ii))
      str(ii)=upper(str(ii));
    endif
  endfor
  out=str;
endfunction
